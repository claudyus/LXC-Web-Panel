# -*- coding: utf-8 -*-
# Example authenticator.
# Class has to have the same name as file (case sensitive) and implement authenticate(self, username, password) method.


class stub:
    def authenticate(self, username, password):
        return True
